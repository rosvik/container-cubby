[[containers]]
name = "container-cubby"
image = "cubby.no/rosvik/container-cubby:main"
ports = ["8602:8602"]
env = [
  "USERNAME=admin",
  "PASSWORD=hunter2",
  "PORT=8602",
  "HOST=0.0.0.0",
  "DATA_DIR=/data",
  "AUTH_MODE=write_only",
  "PRUNE_CRON=* * 2 * * *",
]
mounts = ["data:/data"]
